global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'squid'
    static_configs:
      - targets: ['localhost:9301']

  # Add more exporters or Loki if needed
